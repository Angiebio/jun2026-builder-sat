# secrets — never commit
.env
.env.*
!.env.example
*.key
*.pem
*secret*

# python
__pycache__/
*.py[cod]
.venv/
venv/
.uv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# runtime output — but KEEP the 4 demo run dirs so the web UI works on a fresh clone
runs/*
!runs/pinwheel/
!runs/punched_cards/
!runs/ti_82/
!runs/nokia_3590/
*.log

# node / build
node_modules/
dist/

# os / onedrive
.DS_Store
Thumbs.db
desktop.ini
*.tmp

# local demo inputs — kept local, NOT part of the judged deliverable
test images/
