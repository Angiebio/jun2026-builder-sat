# 🥔 Antique Infernal Engine — Field Guide

## IBM 80-column punched cards (FORTRAN program deck)
*80-column rectangular-hole format introduced 1928; this FORTRAN deck ~1960s-1970s · path: `punched_card_parallel_binary` · confidence 85%*

### Can it Run AI?
❌ No — never, by itself.

> Time to one AI token: **Never (∞)** — it stores/much-too-slowly computes.

**What it actually did:** Passive storage medium. 80 columns x 12 rows = 960 hole positions; in Hollerith/character mode each column encodes one of 80 characters (80 bytes / 640 bits per card). The card stores program or data and performs no computation itself -- a separate card-reader peripheral feeds it to a computer.

**How we bully it into powering AI:** we treat its operations per second as the only resource it has, and ask the absurd question rigorously.

| Equivalent | |
|---|---|
| 🥔 Potatoes | 0 |
| 🚴 Cyclists | 0 |

**The math (frozen — the writer never edits it):**
```
Compute path has 0 ops/sec.
Sentinel -1.0 means this artifact never completes the AI Hello operation target by itself.
```

**Historical note:** An 80-column card holds 80 characters; 80 columns x 12 rows = 960 bit positions.; The IBM 80-column rectangular-hole format was introduced in 1928.

**Gotcha:** It stores or symbolizes computation; it does not perform general AI inference. The first data-center job in history was a no-show.

*The humor is decorative. The math is structural.*
